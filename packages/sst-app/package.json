{
  "name": "@sst-openid-wrapper/sst-app",
  "version": "0.1.0",
  "private": true,
  "main": "stacks/index.js",
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy --stage prod",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ],
    "ignorePatterns": [
      "dist-lambda/*"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.48.0",
    "@serverless-stack/resources": "0.48.0",
    "dotenv": "^10.0.0"
  }
}
